# 鸿蒙智行订单分析

## 逐日外部预测导入

小订、首销、平销的“预测结论”均提供本地导入入口，支持 .xlsx（第一个可见工作表）和 UTF-8 CSV。表头为“车型（代际）、日期、预测数量”；日期为 YYYY-MM-DD 或 Excel 日期，数量必须是非负整数。可下载 CSV 模板。

只导入当前车型的记录，按维护的阶段日期分组：小订＝小订量，首销＝总大定，平销＝交车锁单。导入卡片位于各阶段预测曲线下方。默认只统计当前日期起的待预测日期，也可选择“整个阶段（包括已过日期）”作为完整阶段外部对照。零值有效；重复日期、无效日期、负数或空数量拒绝整次导入，保留上次结果。导入仅作独立对照，不覆盖实际数据或算法结果。默认合并：同车型、同阶段、同日期更新数值，其他记录保留，并逐行记录来源文件；“替换该车型全部外部预测”会先要求确认，替换当前车型所有阶段的导入记录。单文件最多5MB、20000条记录，同车型合并后也最多20000条。

人工修改参数、外部预测和各阶段导入范围自动保存在当前浏览器本机（按HTML文件路径区分），刷新或重新打开同一路径可恢复，不上传。只恢复人工修改项；真实订单和未修改的自动参数仍使用本次生成数据，避免旧快照覆盖新数据。在“数据来源与时间口径”中可清除当前车型草稿、恢复系统参数，外部导入不受影响。清除导入则同步清除本机保存的外部预测。浏览器限制、空间不足或保存记录损坏时给出提示，不阻断预测；更换浏览器、改变文件路径或清理浏览器存储后需重新设置/导入。

车型与时间参数保持直接展开。顶部至“预测结论 / 预测依据 / 预测打分”采用紧凑布局：宽屏七列参数、中等宽度四列、手机两列；生成时间、字段标签和键盘焦点提示保留。

首销总大定不扣大定退订，锁单单独计算。小转大率为累计小转大除以总小订；实际小订退订率仅显示已有真实值，未来缺失不按0显示。预测依据中的真实区间用实线、估算和预测区间用虚线。

阶段未开始也可预测：小订预测作为首销方法二输入；方法一具备真实进度后才可用。平销开始前优先承接可用的首销方法一，否则方法二，以直接大定乘锁单率估算平销量级；平销开始后维持本车型真实平销锁单优先。预测输入始终标注为预测，不混作真实订单。

读取 Excel 汇总报表，生成一个可离线打开的交互式 HTML 看板。品牌、代际、可用标签和底表全部由 Sheet 自动识别；底表保留 Excel 单元格显示格式，支持搜索、CSV 导出和来源跳转。

## 销量数据汇总与取值优先级

`鸿蒙智行销量数据汇总.xlsx` 由 `tools/refresh_sales_forecast_data.py` 生成，网页销量预测消费同一套解析结果与阶段快照。汇总“当前订单逐日”中的首销记录与首销参考曲线用途不同：前者提供当前车型实际进度，后者提供参考完成率、分时形状和参数。不能因数值相同就混用。

### 同类多文件合并

输入目录下所有匹配类型的 `.xlsx` 都参与读取，忽略 `~$` 临时文件。同类不同年份的文件按原文件路径排序（同目录即文件名顺序），不是按修改时间选“最新文件”。

- 已识别表型按车型/Sheet、指标标签及日期或周期对齐；同类 Sheet 名仅忽略空格差异，保留不同首销期标识。订单7级转化按周期、分时小订按日期和小时、日度退订按日期识别记录键，不按固定列号拼接。选配金保留月份与金额档位两层表头，每月汇总列不移到其他月份。
- 同一业务位置取排序后首个非空值，后续文件只补空白。真实 `0` 有效，不被后续非零值覆盖；相同值去重，不重复累加。图表数据同样先完成空白回退，再按原展示规则处理仍无数据的位置。
- 重叠位置数值冲突时保留首个非空值并记录“多文件数值冲突”日志。不会把年度文件中的总计相加，也不会自动把不同首销期合并成一场活动。
- 不同日期取并集，来源保留所有参与文件；底表中心仍可查看原始文件。未知表型保留各原表，不强行套用合并规则。
- 同代际跨年平销文件合并成一份历史，避免被当作多辆参考车重复加权。

### 按字段选择来源

“来源优先级”指某个字段的有效值顺序，不是只选一个文件后放弃其他来源。以下“整理表”均指《小订及首销数据整理》及其二次处理参考数据。

| 用途/阶段 | 优先级及边界 |
| --- | --- |
| 同车型、同日期的真实小订 | 《小订选配比例分析》代际 `by天` →《小订退订分析》小订分时按日汇总→整理表真实“小订by天”。历史回退还要求同一小订开始日期及维护窗口。 |
| 小订 D1 当前分时 | 《小订退订分析》“小订分时退订”；日合计不能冒充分时快照。 |
| 首销未开始 `before` | 《小订退订分析》→整理表。小订结束后冻结总量也先使用这个阶段的总小订口径，再回退当前小订历史总量、可用真实日合计。 |
| 首销进行中 `active` | 《首销期订单节奏》→整理表。该阶段通用字段回退不包含“小订退订分析”；小订分时、逐日小订及退订原始记录仍单独保留。 |
| 首销已结束 `ended` | 整理表→《首销期订单节奏》→《小订退订分析》，只回退各来源实际提供的字段。退订表不能提供首销大定/锁单。 |
| 首销阶段未知 `unknown` | 诊断取数按《首销期订单节奏》→整理表→《小订退订分析》；日期不明仍不能正常预测。 |
| 平销真实销量 | 《锁单选配比例分析》代际 `by天` 的“交车锁单”；参考完整周取 `by周` 的“交车锁单”。不再读取独立平销历史文件。 |

首销日明细先选优先级最高且含数量数据的来源作为日期骨架，再分别补大定、净大定、小转大、直接大定、锁单和累计小订退订。优先按绝对日期匹配；只有一侧无有效日期时才按 Dn/位置回退，不能把不同日期的 D1 互相覆盖。此处是沿骨架补字段，不是把各来源所有日期无条件并入。

日数量必须是有限非负数，真实 `0` 可用；空白、非法数和负数按缺失处理并继续回退。分项与总大定明显不一致时将分项标记为缺失，不能继续充当真实结构。总小订作为预测分母另有要求：必须大于 `0`；因此“0有效”的日数量规则不表示总小订为0也可以做除法预测。

总小订仍遵循阶段来源优先级；在首销节奏来源内部优先使用有效显式量，缺失时可能由累计小转大/转化率反推；退订表则由累计退订/退订率稳定反推分母。反推量会记录估算原因，不能据此声称逐日小订真实可用。已有整理表显式总小订时，不使用首销节奏的反推总小订抢占它。首销结束后的锁单终值，仅在同车型、同首销开始日期、同窗口天数、总大定一致且锁单量有效时采用整理表终值，否则回退完整真实日锁单合计。

名称匹配优先历史传播名精确命中；没有精确命中时只接受唯一代际别名，避免同代际不同版本或传播事件串用。字段来源、退订来源和数据状态随汇总保留，可在来源目录、底表和日志核对。

### Excel 缓存的读取边界

`core/excel.py::load_data_workbook` 在缓存未命中时使用普通 `openpyxl` 读取（`data_only=True, read_only=False`），再缓存非空值、数据类型、数字格式和 Sheet 状态。合并区域只有左上角保留值，其他位置仍为空；不使用会暴露合并区域底层残留 XML 值的流式读取。缓存视图不保留合并样式、图片等装饰信息，也不保留纯样式空白形成的巨大网格；保证的是数据取值语义，不是 Excel 排版复刻。

缓存签名包含版本、完整路径、文件大小和纳秒级修改/创建时间。旧版本、源文件变化、损坏或缺少结束标记的缓存均重新读源文件；写入采用临时文件替换，读取期间源文件变化则不提交缓存。缓存不能计算 Excel 公式，`data_only=True` 读取的仍是文件中已有的公式缓存结果。

## 销量预测逻辑与真实数据边界

### 时间、参考与真实性

判定日期为 `forecast_as_of_date`，未配置时用生成当天。首销开始优先取整理表维护窗口，再取首销节奏日期、历史参考日期；明确起止日期优先于天数，缺截止日且有维护天数时才推导截止日。小订起止日期必须维护，缺结束日期不猜成1天；平销开始为首销截止次日。开始日和结束日都属于阶段内，结束日当天仍是进行中，次日才算已结束。

`as_of_date` 只改变阶段判定和日期切分，不还原当时文件版本，也不自动撤销后来补录的数据。历史复盘若要严格使用当时已知的信息，需要配套当时的原始文件快照。

| 数据状态 | 参与方式 |
| --- | --- |
| 已结束日（日期早于判定日，且在阶段内） | 冻结真实数量。必须通过日期完整性、非负数及分项校验；必要数据缺失时停止对应预测，不用参考占比或预测补成真实日。 |
| 当日快照 | 只代表截至某时点已发生量；全天外推仍为估算，不计入完整日真实累计。当日预计量不得低于已发生量。 |
| 未来日期 | 预测输出或参考数据，不能提前算入真实累计。 |
| 非真实参考曲线 | 用于形状/完成率，例如“小订进度”回退曲线；不能替代目标车型真实逐日数据。 |

小订和平销稀疏日期只在两个有效观测日期之间补缺省零销量日；源表显式空白、覆盖范围之外日期仍是缺失。平销周参考排除首销重叠周及当前未结束周，周序列不连续时不参与周环比，保留可用真实日。

主辅参考按各自权重对有效指标重新归一加权。小订评分考察档位、能源、发布类型、窗口天数、已知小订量级；平销评分考察档位、能源、发布类型、首销锁单率。这两类自动推荐至少需3项有效评分证据，证据不足可人工选择；参考不选当前车型本身。首销按分时、转化、直接大定占比、锁单率、小转大/直接大定完成率和日曲线等任务分别选择参考，不能认为所有公式共用一辆参考车。

### 小订预测

| 阶段 | 算法 |
| --- | --- |
| 小订前 | 对每个参考车，优先用“当前线索量 × 参考最终小订/参考线索量”折算量级，线索不可用时用参考最终小订；双方热度有效时再乘 `sqrt(当前热度/参考热度)`，热度系数限制在0.6～1.6。主辅加权得最终总小订，D1＝总量×参考D1占比（小订前缺该占比时默认20%）。 |
| D1进行中 | D1预计全天＝当前分时累计÷参考同小时完成率；最终总小订＝D1预计全天÷参考D1占比。仅1天窗口直接用D1量。参考分时完成率限制在3%～100%；缺当前分时、真实参考分时或有效D1占比时停止，不用通用线性曲线替代。 |
| D1以后、仍在小订期 | 先检查D1至昨天的全部真实日，再以“完整日真实累计÷参考同期累计完成率”推算终局，且终局不低于真实累计。无需先知道目标车型最终总小订。 |
| 小订结束 | 按前述冻结总小订优先级收口，不再预测未来余量。逐日形状优先真实“小订by天”，回退“小订进度”时仍注明其为参考形状；没有正数可冻结总量时停止。终值可用不代表逐日完整，结束分支按总量收口，不能把参考形状当作补齐的真实日。 |

参考完成率按目标窗口重定基：取参考第 n 日累计量÷参考目标截止日累计量；参考天数不足时延伸曲线并标为外推。真实曲线优先，标准化“小订进度”仅作回退。剩余量按参考日增量、目标日历系数和衔接权重分配；当天已发生小订高于原预测时，先重分配未来余量，余量不足才抬升总量。

### 首销预测：两种方法独立计算

首销口径为**总大定＝小转大＋直接大定**，不扣大定退订；交车锁单另算。小转大率＝累计小转大÷总小订，直接大定占比＝直接大定÷总大定。

**方法一：按真实进度反推终局。** 分别计算：

```text
小转大终局 = max(小转大基准量, 小转大基准量 / 参考同期小转大完成率)
直接大定终局 = max(直接大定基准量, 直接大定基准量 / 参考同期直接大定完成率)
总大定终局 = 小转大终局 + 直接大定终局
```

基准量＝已结束日真实累计＋当日存在快照时的全天估算。页面真实累计只显示前一部分。参考完成率先按目标首销窗口重定基，再主辅加权；系统参考完成率过低时设0.5%安全下限并提示，也支持人工覆盖完成率。方法一需有有效真实进度、完整已结束日期和可用参考完成率，首销前不可用。

**方法二：由小订和转化参数推算终局。** 总小订为 S、小订转化率为 c、直接大定占比为 p：

```text
原始小转大终局 = S × c
原始直接大定终局 = 原始小转大终局 × p / max(1 - p, 0.05)
总大定终局 = 小转大终局 + 直接大定终局
预计锁单 = 总大定终局 × 大定到锁单率
```

系统参数取各任务主辅参考加权值，也可人工调整。小转大、直接大定各自不能低于对应基准量，锁单不能低于已发生真实锁单。首销尚未开始且小订阶段尚未结束时，会将当前车型、当前判定日的小订预测接入方法二，明确标为预测输入。总小订非正、转化率不可用或原始数据校验失败时不输出方法二结果。

首销前D1可人工填写；系统建议为方法二总大定×历史D1占比，占比限制在8%～75%，无有效比例时默认30%。D1已结束后使用真实值，不再接受D1预测替代。进行中的当天优先 `by时`，否则可用 `by天` 当日快照估算全天并提示低置信度；只有当日允许按参考占比拆分缺失分项，并保持小转大＋直接大定＝总大定。

D2以后分时完成率优先本车型最近最多7个完整非D1日、同日历类型优先的真实分时曲线；否则借用历史D1，仍缺失时首销可用通用线性曲线并提示低置信度。首销日形状缺参考时可用通用幂衰减回退。这些只影响估算，不豁免已结束日真实数据校验；与小订D1缺分时直接停止的规则不同。

两种方法各自保留完整真实日和当日估算，分别分配自己的剩余小转大、直接大定；整数逐日合计与各自终局一致。首销结束且真实日完整时，两种方法都按真实结果收口，不再分配余量。累计小订退订只展示真实已知值，未来缺失不显示成0，也不当作“当日大定退订”。

### 平销预测：只输出交车锁单

量级和趋势按以下顺序确定：

1. **平销尚未开始且首销预测可用**：优先承接首销方法一，否则方法二，用预测直接大定×同期大定到锁单率折算平销输入；它仍是预测锁单量级。
2. **本车型已有平销实际**：优先最近最多7个完整真实日的日历还原日均锁单；有前后两组完整7日且前组均值大于0时计算环比，否则趋势按持平。日基准不可用时可取本车型最近最多4个完整周的日历还原日均量及最近最多4个有效周环比。
3. **本车型已有已结束首销日，但暂无平销基准**：取首销末端最多14个完整日，以“该日直接大定×样本累计锁单/累计总大定”折算，剔除日历系数后用最近最多7日均值作为量级；两组7日形成趋势，不足时持平。首销样本缺字段/日期或总大定为0时停止，不跳过错误静默改用参考车。
4. **首销还没有已结束日且上游预测不可用**：使用主辅历史车型完整周锁单量级和趋势。参考不足则停止。

计算当前周起连续4个自然周的滚动区间；若平销尚未开始，从平销开始日所在周起算并剔除开始日前日期，因此首周可能不足7天。

```text
日趋势倍率 = 周环比 ^ (1/7)
滚动目标 = 四周区间内 Σ(日历还原日均量 × 日趋势倍率^距区间首日天数 × 该日日历系数)
待预测余量 = max(滚动目标, 区间内已实现锁单) - 区间内已实现锁单
```

以前一天真实交车锁单衔接，将余量分配到今天及未来日期，再按自然周汇总为“实际＋预测”。当天快照是下限，必要时重分配未来余量或抬升总量。即使图表仅展示滚动4周，也必须校验**平销开始日至昨天的全部真实日**；任何必要日缺失/异常或缺昨天真实锚点都会停止，不用预测覆盖窗口外的历史缺口。

### 三阶段共用的逐日分配

小订、首销、平销分别维护日历系数，默认工作日1、普通周末1.15、法定放假1.3，人工值必须大于0且不超过5；调休上班按工作日，超出已维护官方日历年度按自然周估计并提示。

先用参考形状、日历系数和已知锚点形成基础曲线，再按渐进差额权重分配剩余总量；默认权重为首个待预测日0、第二日0.5、以后各日1，结合基础量计算有效分配权重。小订锚点可为最近完整真实日或D1全天估算；首销以最近完整真实日衔接；平销开始后用昨天真实锁单。整数分配保持非负及合计一致，真实日不随参数改变。用户输入差额权重时，数量必须覆盖全部待预测日期且均非负；无效参数停止分配并提示。

网页人工参数和外部逐日预测仅作本机预测/对照，不回写原始Excel、不覆盖真实订单，也不将估算写成汇总中的已发生真实值。

## 运行

以下命令在 `generate_html` 文件夹中运行。推荐路径为 `订单分析/scripts/generate_html`，默认数据根目录为 `订单分析`。代码仓库根目录名为 `scripts` 时取它的上一级作为数据根目录；其他目录名称则以代码仓库根目录作为数据根目录。

```powershell
python main.py
```

生成完成后会自动使用系统默认浏览器打开 HTML。若只想生成、不自动打开：

```powershell
python main.py --no-open
```

指定输入输出：

```powershell
python main.py --input "..\..\output_file" --output "..\..\output_file\鸿蒙智行订单分析汇总.html" --check
```

### 刷新销量预测二次处理数据

原始历史数据和传播名—代际名映射放在数据根目录的 `input_file\销量预测输入文件`；自动生成的销量数据汇总放在 `output_file\鸿蒙智行销量数据汇总.xlsx`。刷新脚本放在 `generate_html\tools`，可在 `generate_html` 文件夹中运行，或在 PyCharm 中直接运行该脚本：

如需按历史时点复盘销量预测，可在 `config.json` 的 `forecast_as_of_date` 填写 `YYYY-MM-DD`；留空则使用运行当天。该配置只改变阶段判定及已发生/未来日期切分，不修改原始订单数据。

```powershell
python tools/refresh_sales_forecast_data.py
```

刷新脚本检查历史文件、车型基本信息、当前订单文件清单与修改时间、转换代码及汇总日期。新增、删除或更新当前订单文件、修改复盘日期或跨日运行都会重新汇总；依赖未变化时跳过。需要无条件重建时使用：

```powershell
python tools/refresh_sales_forecast_data.py --force
```

脚本读取历史整理表、车型基本信息，以及当前订单目录中的《首销期订单节奏》《小订退订分析》《小订选配比例分析》《锁单选配比例分析》，生成 `output_file\鸿蒙智行销量数据汇总.xlsx`。汇总将当前阶段并入车型基本信息，将历史小订与当前小订退订合并为统一逐日表；同车型同日期的小订按《小订选配比例分析》代际by天→《小订退订分析》分时汇总→历史小订by天的顺序读取可用值。数据来源目录对应原始文件和汇总位置。网页销量预测统一从汇总文件读取，继续按原有阶段优先级逐字段回退。空值、真实0、日期和数值格式保持原义。

更新原始Excel后运行main.py即可刷新。汇总中的明细为生成时快照，请在原始文件修改业务数据；直接编辑生成明细会在刷新时被覆盖。网页参数仍在网页人工调整，不将预测值写成已发生真实订单。单独运行刷新脚本可用 `--orders 当前订单目录` 和 `--as-of-date YYYY-MM-DD`。旧名称仅保留兼容读取。

预测汇总仅收录销量预测需要的数据，不重复复制 SKU、选配金等原始明细。累计小订退订统一放在“小订及退订逐日”，不混为当日大定退订；逐日表区分已结束日、当日快照、未来日期及非真实参考曲线。小订和平销的稀疏日期仅在两个有效观测日期之间补缺省的零销量日期，原表明确留空的单元格及覆盖范围以外的日期仍按缺失处理。

历史参考曲线先按历史传播名精确匹配；无精确命中时只接受唯一代际别名，避免同代际不同版本串用曲线。首销已结束时，同车型、同首销窗口且总大定一致的整理表锁单终值优先于逐日回退值。首销“已结束日真实小转大/直接大定”不包含当天全天估算。当天只有大定总数而缺少分项时，分项仅作估算且保持加总一致；两种方法的整数终局与逐日合计一致。D2以后分时滚动优先用本车型最近最多7个完整非D1日、同日历类型优先的分时曲线；否则借用历史D1并提示低置信度。分时比例的分母为同日实际总量或当日预计全天量，不混用其他日期。小订和平销未结束日的已发生量是预测下限，不计为完整真实日。

汇总生成在内存中完成来源整合、计算和压缩快照，再写入最终文件，避免把中间原始Sheet反复保存、重读；最终文件仍通过临时文件原子替换，刷新签名与跨日失效规则不变。

如需每次运行 `main.py` 前自动刷新，将 `config.json` 中以下配置改为 `true`：

```json
"refresh_sales_forecast_data_before_build": true
```

`build_manifest_mode` 控制诊断清单：`off` 为默认高性能模式，`compact` 为紧凑 JSON，`full` 为带缩进的调试格式。

输出文件：

- `output_file\鸿蒙智行订单分析汇总.html`：最终网页（以下均相对于数据根目录）
- `output_file\build_manifest.json`：可选的解析后页面数据；仅在 `build_manifest_mode` 设为 `compact` 或 `full` 时生成
- `output_file\log\generate_html_YYYYMMDD_HHMMSS.log`：完整运行日志，包含文件读取、主体/模块映射、兼容处理、校验告警和错误堆栈；所有运行日志直接放在 `log` 目录，不再按运行创建子目录

## 依赖

- Python 3.10+
- openpyxl 3.x

详细代码导航和 AI 修改规则见 `README_AI.md`。
